<!--
Every commit message must be a Conventional Commit, e.g. "feat: add Utrecht
Centraal" — commitlint checks each one in CI. PRs are merged with a merge commit
(not squashed), so the branch commits are what semantic-release releases. The
Release preview comment shows the version the merge will publish.
-->

## What & why

<!-- What does this change and what problem does it solve? Link any issue. -->

Closes #

## Checks

- [ ] `npm run format` `npm run lint` `npm run typecheck` `npm test` `npm run build` pass
- [ ] Commit messages are Conventional Commits
- [ ] Tests added/updated for behaviour changes
- [ ] Checked on mobile and desktop, light and dark (if UI)
- [ ] Docs / README updated (if needed)
